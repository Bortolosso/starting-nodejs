{{#each error}}
    <div class="alert alert-danger">{{text}}</div>
{{else}}

{{/each}}

<h3>New categorie: </h3>
<div class="card">
    <div class="card-body">
        <form action="/admin/categories/add" method="POST">
            <label for="name">Name: </label>
                <input type="text" id="name" name="name" placeholder="Name of categorie" class="form-control">
            <label for="slug">Slug: </label>
                <input type="text" id="slug" name="slug" placeholder="Slug of categorie" class="form-control"><br>
            <button type="summit" class="btn btn-success">Create categorie</button>
        </form>
    </div>
</div>